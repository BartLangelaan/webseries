language: node_js

# Only most stable nodejs needed
node_js: stable

# Give permission to install meteor
sudo: required


before_install:
  # Install Meteor
  - "curl -L https://install.meteor.com/ | /bin/sh"
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
  - npm install -g gulp